# generated by datamodel-codegen:
#   filename:  search_request.v1.schema.json

from __future__ import annotations

from typing import Literal

from pydantic import BaseModel, ConfigDict, Field, conint, constr


class SearchRequestV1(BaseModel):
    """
    Query parameters accepted by loom-server GET /search, expressed as an object so the same contract validates client and server.
    """

    model_config = ConfigDict(
        extra='forbid',
    )
    schema_: Literal['search_request.v1'] = Field(
        ...,
        alias='schema',
        description='Contract discriminator, always search_request.v1',
    )
    query: constr(min_length=1, max_length=512) = Field(
        ..., description='User query string'
    )
    vertical: (
        constr(pattern=r'^[a-z0-9][a-z0-9_-]*$', min_length=1, max_length=64) | None
    ) = Field('devdocs', description='Vertical pack id to search (defaults to devdocs)')
    offset: conint(ge=0, le=1000) | None = Field(
        0, description='Result offset for pagination'
    )
    limit: conint(ge=1, le=50) | None = Field(
        10, description='Number of results to return'
    )
